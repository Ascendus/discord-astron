import DiscordAstron from "../src/index";

export default DiscordAstron;

export const {
    AstronClient,
    Category,
    Command,
    CommandHandler,
    Configuration, 
    Listener, 
    ListenerHandler,
    Utilities,
    version
} = DiscordAstron;