import DiscordAstron from "../src/index";

export default DiscordAstron;

export const {
    AstronClient, 
    Category,
    Command,
    CommandHandler,
    Configuration, 
    GuildPrototype,
    Listener, 
    ListenerHandler,
    Utilities,
    MongooseProvider,
    Provider,
    SequelizeProvider,
    SQLiteProvider,
    version,
    description,
    homepage,
    npmURL,
    githubIssuesURL,
    keywords,
    author,
    publisher,
    license,
    dependencies
} = DiscordAstron;